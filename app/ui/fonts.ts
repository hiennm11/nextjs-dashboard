import { Inter, Playwrite_DK_Loopet, Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const playwright = Playwrite_DK_Loopet();
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});